export type SuppliersCard = {
  order: number;
  title: string;
  color: string;
  marginTop: string;
  marginTopTablet: string;
};

export type SuppliersData = {
  data: {
    [key: string]: string;
  };
};
